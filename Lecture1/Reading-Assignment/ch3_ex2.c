#include <stdio.h>

int main(void) // calling the function main as an integer
{
    int i; // specifying variable i as an integer
    float x; // specifying variable x as a float

    // assigning values to each variable
    i = 40;
    x = 839.21f;

    printf("|%d|%5d|%-5d|%5.3d|\n", i, i, i, i}; // prints and displays each of the values inside the || characters, with different spaces
    printf("|%10.3f|%10.3e|%-10g|\n", x, x, x}; // prints and displays each of the values inside the || characters, with different spaces and values depending on their conversion specifier

    return 0; //executes program
}
