#include <stdio.h>

int i, j;  //specifying the variables i and j as integers

float x, y; //specifying the variables x and y as floating numbers

// assigning values to each variable
i = 10;
j = 20;
x = 43. 2892f;
y = 5527.0f;

printf("i = %d, j = %d, x = %f, y = %f\n", i, j, x, y); // prints and displays the values of each variable, with their conversion specifier
